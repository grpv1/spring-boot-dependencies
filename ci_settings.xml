<?xml version="1.0" encoding="UTF-8"?>
<settings xmlns="http://maven.apache.org/SETTINGS/1.0.0" 
          xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" 
          xsi:schemaLocation="http://maven.apache.org/SETTINGS/1.0.0 http://maven.apache.org/xsd/settings-1.0.0.xsd">
 
  <pluginGroups>
  </pluginGroups>
          
  <proxies>
  </proxies>
          
  <servers>
    <server>
      <id>github-maven</id>
      <!--username>${env.github_repository_owner}</username-->
      <username>${env.github_access_user}</username>
      <password>${env.github_access_token}</password>
    </server>
    <!--server>
      <id>gitlab</id>  
      <username>${GITLAB_USER_LOGIN}</username>  
      <password>${GITLAB_MAVEN_PRIVATE_TOKEN}</password>  
    </server-->
  </servers>

  <mirrors>
  </mirrors>  
 
  <profiles>
    <!--profile>
      <id>gitlab</id>
      <repositories>
      
      </repositories>
      <pluginRepositories>
        <pluginRepository>
         
        </pluginRepository>
      </pluginRepositories>
    </profile-->
  </profiles>

  <!--activeProfiles>
      <activeProfile>gitlab</activeProfile>
  </activeProfiles-->

</settings>
